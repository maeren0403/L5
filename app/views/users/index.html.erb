<% @users.each do |e| %>
<p>
    <%= e.uid %>
    <%= e.pass %>
    <%= link_to '削除', user_path(e.id), method: :delete %>
</p>
<% end %>

<p>
<%= link_to 'ユーザー追加', new_user_path %>
</p>

<%= link_to 'ツイートページ', tweets_path %>